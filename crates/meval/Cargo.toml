[package]
name = "meval"
version = "0.3.0"
edition = "2024"

[dependencies]
serde = { version = "1", optional = true }

[dev-dependencies]
serde_derive = "1.0.228"
serde_json = "1.0.149"

[features]
default = []
serde = ["dep:serde"]

[package.metadata.docs.rs]
features = [ "serde" ]
